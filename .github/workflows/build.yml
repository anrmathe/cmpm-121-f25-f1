name: Build LÖVE package

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Build .love package
        uses: love-actions/love-actions-core@v1
        with:
          build-list: |
            .
            !tests/
            !.vscode/
            !.github/
          package-path: ./build/sudoku.love
